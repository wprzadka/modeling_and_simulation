//
// Created by viking on 15.05.21.
//

//#include "../../includes/solver/Euler.h"

//template <int N>
//std::array<sf::Vector2<long double>, N> Euler::solve(std::array<sf::Vector2<long double>, N> prevValue, std::array<sf::Vector2<long double>, N> derivative, float stepSize){
//    std::array<sf::Vector2f, N> nextValue;
//    for (int i = 0; i < N; ++i){
//        nextValue[i] = prevValue[i] + stepSize * derivative[i];
//    }
//    return nextValue;
//}
